# 20260819 mdev 剩余未同步内容审查

## 0. 收口状态（2026-08-19 更新）

本审查文档列出的剩余项，现已全部完成同步。

本次最终收口完成的内容包括：

1. 组织树前端整块：
   - `frontend/src/components/OrganizationSwitcher.vue`
   - `frontend/src/components/OrgTreeSelector.vue`
   - `frontend/src/views/admin/AdminLayout.vue`
   - `frontend/src/views/admin/OrgTreeManage.vue`
   - `frontend/src/views/admin/MemberManage.vue`
   - `frontend/src/views/admin/components/*` 配套子组件
   - `frontend/src/stores/orgTree.ts`
   - `frontend/src/api/org-tree/index.ts`
   - `frontend/src/stores/organization.ts` / `frontend/src/stores/auth.ts` / `frontend/src/router/index.ts` / `frontend/src/stores/menu.ts` / `frontend/src/components/menu.vue` 接线
2. `internal/middleware/super_admin.go` 已按 `dev` 文件级回补。
3. 品牌批次 `e4960695` 已完成同步：
   - `config/prompt_templates/*.yaml` 品牌自称与角色说明
   - `frontend/index.html`
   - `frontend/src/assets/img/screenshot-1.svg`
   - `frontend/src/i18n/locales/{zh-CN,en-US,ko-KR,ru-RU}.ts`
4. 登录页 [frontend/src/views/auth/Login.vue](frontend/src/views/auth/Login.vue) 已按 `dev` 的 IntraMind 暗色风格对齐，并保留当前分支既有的邀请注册 / OIDC / 语言切换逻辑。

当前结论应更新为：

1. `20260819-02` 方案中的剩余未同步工作已清零。
2. 当前 `mdev` 已完成权限主线、组织树前端、密码链路、本地运行脚本和品牌批次的同步收口。

下文保留为本次收口前的审查记录。

## 1. 审查范围

本审查以以下两份文档为基线：

1. [my_docs/20260819-02-mdev分支权限主线同步方案.md](my_docs/20260819-02-mdev%E5%88%86%E6%94%AF%E6%9D%83%E9%99%90%E4%B8%BB%E7%BA%BF%E5%90%8C%E6%AD%A5%E6%96%B9%E6%A1%88.md)
2. [my_docs/20260819-03-mdev分支详细同步执行计划.md](my_docs/20260819-03-mdev%E5%88%86%E6%94%AF%E8%AF%A6%E7%BB%86%E5%90%8C%E6%AD%A5%E6%89%A7%E8%A1%8C%E8%AE%A1%E5%88%92.md)

同时结合当前仓库实际文件状态做复核。

---

## 2. 结论总览

截至本次审查，`20260819-02` 方案中的权限主线后端、知识库与文档权限语义、智能体/IM 运行时知识范围、IM 渠道 owner 隔离、用户密码链路、本地运行脚本主体都已经完成。

当前仍未完全同步完成的内容，主要集中在两类：

1. 组织树与成员管理的前端大块页面仍未补齐。
2. 本地品牌文案批次 `e4960695` 仍未整批进入，只完成了登录页视觉风格同步。

如果只按“明确缺失的文件级项”统计，当前还剩 **6 个明确未同步项**。

如果按“主题级批次”统计，则还剩：

1. **1 组组织树前端页面批次**
2. **1 组品牌文案批次**

---

## 3. 已完成的主线内容

以下内容可视为已经完成或已具备等价实现：

1. 阶段 0/1：`mdev` 分支、fresh DB、`000085` 到 `000091` 迁移尾巴。
2. 阶段 2：same-tenant KB/Agent 可见性骨架。
3. 阶段 3：后端 org-tree、空间成员管理、super-admin 相关权限链路。
4. 阶段 4：知识库 owner / 文档创建者 / 文档按钮权限语义。
5. 阶段 5：Agent QA、建议问题、IM `/search` `/info` 使用统一 KB scope。
6. 阶段 6：IM 渠道 `created_by` 隔离、built-in agent override 行 `visibility=global`。
7. 阶段 7：
   - 自助修改密码组件化
   - 管理员重置密码组件化
   - `scripts/dev.sh` 诊断增强
   - `scripts/dev-host-data.sh`
   - `scripts/dev-named-volume.sh`
   - compose 数据卷模式参数化
8. 登录页视觉风格已在本次任务中同步到 `dev` 的 IntraMind 暗色风格。

---

## 4. 明确未同步项

### 4.1 组织树前端页面仍缺 5 个文件

当前仓库中以下文件仍不存在：

1. `frontend/src/components/OrganizationSwitcher.vue`
2. `frontend/src/components/OrgTreeSelector.vue`
3. `frontend/src/views/admin/AdminLayout.vue`
4. `frontend/src/views/admin/OrgTreeManage.vue`
5. `frontend/src/views/admin/MemberManage.vue`

这意味着：

1. 阶段 3 的后端组织树与成员管理能力已经落地，但对应的前端完整管理界面仍未同步。
2. 当前成员密码重置能力虽然已在系统设置页和组件层可用，但并不是 `20260819-02` 方案中预期的那套组织树 / 成员管理页完整 UI 形态。

### 4.2 `internal/middleware/super_admin.go` 原文件仍未同步

当前仓库中以下文件仍不存在：

1. `internal/middleware/super_admin.go`

需要注意：

1. 这不代表 super-admin 语义不存在。
2. 当前仓库已经通过 `SystemAdmin` 路由守卫、`IsSystemAdminFromContext`、`org_tree` handler 中的 `IsSuperAdmin` 判断等方式覆盖了主要平台级权限能力。
3. 但如果严格按 `20260819-02` 的文件清单审查，则这个“原文件级同步”尚未完成。

因此它更像是：

1. **文件未同步**
2. **语义大体已被现有实现吸收**
3. **不是当前权限主线的阻塞项**

---

## 5. 品牌批次仍未整批同步

### 5.1 登录页样式已同步，但品牌批次没有整包进入

本次任务已完成：

1. `frontend/src/views/auth/Login.vue` 的视觉风格对齐到 `dev` 的 IntraMind 暗色品牌样式。

但 `20260819-02` 中的品牌批次 `e4960695` 仍未整批同步，证据如下：

1. 当前 `frontend/src` 中仍可检索到 `WeKnora` 文案 **284** 处。
2. 当前 `frontend/src` 中 `IntraMind` / `智枢` 文案仅 **3** 处。

这说明：

1. 登录页已完成局部品牌同步。
2. 但全站文案、标题、说明文本、品牌名替换仍然没有整批落地。
3. 按 `20260819-03` 的执行记录，这一批本来就被明确延后，没有进入当前权限主线收口范围。

### 5.2 因此品牌层面的当前状态应定义为“部分完成”

可以给出如下判断：

1. **登录页品牌视觉：已完成**
2. **全站 IntraMind 文案替换：未完成**

---

## 6. 当前剩余量的准确说法

如果按不同统计口径，当前剩余未同步量如下：

### 6.1 按文件级明确缺口统计

还剩 **6 个明确未同步文件项**：

1. `frontend/src/components/OrganizationSwitcher.vue`
2. `frontend/src/components/OrgTreeSelector.vue`
3. `frontend/src/views/admin/AdminLayout.vue`
4. `frontend/src/views/admin/OrgTreeManage.vue`
5. `frontend/src/views/admin/MemberManage.vue`
6. `internal/middleware/super_admin.go`

### 6.2 按主题级统计

还剩 **2 组未完全收口的主题**：

1. **组织树前端管理页批次**
2. **品牌文案批次 `e4960695`**

### 6.3 按是否阻塞权限主线统计

**不阻塞当前权限主线** 的剩余项主要是：

1. 组织树前端 UI 完整性
2. 品牌文案整站替换
3. `super_admin.go` 的文件级回补

换句话说：

1. 当前 `mdev` 的“权限主线 + 知识库权限 + Agent/IM scope + 密码链路 + 运行脚本”主目标已经完成。
2. 剩余未同步内容主要是前端完整管理界面和品牌化收尾，而不是后端主权限链路缺失。

---

## 7. 建议的后续顺序

如果继续推进，建议顺序如下：

1. 先补齐组织树前端五个页面/组件，让阶段 3 形成完整 UI 闭环。
2. 再视需要单独处理 `internal/middleware/super_admin.go` 是否要按原文件回补，还是继续维持现有等价实现。
3. 最后单开一轮品牌批次，把 `e4960695` 中需要保留的 IntraMind 文案按文件筛选进入，而不要与权限主线继续混做一轮。
